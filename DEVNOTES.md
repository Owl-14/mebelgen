# Инженерные заметки и журнал ошибок (MEBELGEN)

Документ фиксирует **все нетривиальные правки**, **встреченные ошибки** и **как их
не допускать снова**. Читать перед тем, как трогать парсинг Word, 3D-рендер Blender
или композитинг листов.

Легенда: 🔴 ошибка · ✅ решение · ⚠️ правило на будущее.

---

## 1. Окружение и оболочка (PowerShell / Windows)

### 1.1 Оператор `||` не работает в PowerShell
- 🔴 Команды вида `ls -la || echo ...` падают: в PowerShell нет `||` (до PS7).
- ✅ Используем нативные команды: `Get-ChildItem`, `Test-Path`, `Select-Object`.
- ⚠️ Не копировать bash-однострочники с `||`/`&&` в PowerShell. Для цепочек —
  `;` или отдельные вызовы. В этом проекте все шелл-команды пишем под PowerShell.

### 1.2 Кириллица в выводе
- ✅ В начале команд ставим `[Console]::OutputEncoding=[System.Text.Encoding]::UTF8`
  и `$env:PYTHONUTF8=1`, иначе русский текст в stdout/файлах бьётся.

### 1.3 Состояние shell сохраняется между вызовами (cwd!)
- 🔴 После `cd "D:\tools"` следующий `python -c ...` запускался не из корня
  проекта → `ModuleNotFoundError: No module named 'mebelgen'`.
- ✅ Запускать из корня проекта (или передавать `working_directory`).
- ⚠️ Помнить: рабочая директория «липкая». Не полагаться на неё — указывать явно.

---

## 2. Установка Blender (нехватка места на C:)

- 🔴 `winget install Blender` падал: **нет места на C:** (System32, 0.2 ГБ свободно).
- 🔴 Скачивание zip прерывалось (частичная загрузка ~59 МБ).
- ✅ Качаем **portable**-сборку на D:: `curl.exe -L -C - -o D:\tools\blender.zip <url>`
  (`-C -` докачивает с места обрыва), распаковка `tar -xf`.
- ✅ Итог: `D:\tools\blender-4.2.22-windows-x64\blender.exe` (Python 3.11 внутри).
- ⚠️ На этой машине C: почти полон — всё тяжёлое (Blender, рендеры) держим на D:.
- ⚠️ Драйвер ищет Blender в `D:\tools\blender*`, `Program Files`, `PATH`,
  `MEBELGEN_BLENDER` (см. `render/blender_render.py: find_blender`).

---

## 3. Растровый экспорт SVG → PNG

- 🔴 `cairosvg` на Windows: `OSError: no library called "cairo" was found`
  (нужны системные нативные библиотеки Cairo).
- ✅ Основной конвертер — **`resvg-py`** (самодостаточный, на Rust, без системных
  зависимостей). `cairosvg` оставлен как опциональный фолбэк.
- ⚠️ Не завязываться на системный Cairo. Для встраивания растра в SVG resvg
  поддерживает `<image>` c `data:image/png;base64,...` — этим и пользуемся.

---

## 4. Извлечение картинок из Word

### 4.1 Картинки лежали как VML, а не DrawingML
- 🔴 Сначала искали только `a:blip/@r:embed` (DrawingML) — нашли **0** картинок.
  При конвертации `.doc → .docx` через MS Word изображения часто сохраняются как
  **VML**: `v:imagedata/@r:id`.
- ✅ Сканируем **оба** формата (см. `parsing/doc_reader.py: _iter_rids`):
  - `a:blip` → атрибут `r:embed` (или `r:link`),
  - `v:imagedata` → атрибут `r:id`.
- ⚠️ Для любых legacy-docx всегда проверять VML-ветку. Картинку товара берём из
  колонки «Изображение» (определяется по заголовку `изображ…`, фолбэк — колонка 2).

### 4.2 Резолв байтов картинки
- ✅ Через `document.part.rels[rid].target_part.blob`. Расширение — из
  `target_part.partname`. Сохраняем в `media_rows/` рядом с docx.

---

## 5. Анализ формы по фото (Pillow)

- Задача: понять, что изделие **круглое** и **рифлёное (гофра)**, когда в тексте
  это не указано (кофейный стол `400×400×400` оказался рифлёным цилиндром Ø400,
  а не кубом).
- ✅ `parsing/image_analysis.py`:
  - фон оцениваем по 4 углам, маска объекта по порогу отличия от фона;
  - **рифление** — считаем вертикальные полосы (локальные максимумы яркости) на
    нескольких горизонтальных срезах; ≥7 пиков ⇒ флютинг ⇒ цилиндр;
  - **круг** — кривизна верхней границы силуэта (центр выше краёв).
- ⚠️ Эвристика «шумная»: бывают ложные срабатывания (тумба определилась как
  `round`). Поэтому **применяем cue только к малым столам** и только если форма
  ещё не определена: `spec_parser.apply_image_cues` трогает лишь товары с
  «кофейн»/«журнальн» в названии и без ящиков. Не распространять на шкафы/столы.

---

## 6. Blender: выбор движка рендера

- 🔴 **Eevee Next** падает headless на интегрированной AMD Vega:
  `Compute shader … HW_UNSUPPORTED`, затем `EXCEPTION_ACCESS_VIOLATION` (краш).
  Eevee Next требует compute-шейдеры/возможности GPU, которых нет у драйвера.
- ✅ Перешли на **Cycles, device = CPU**. Работает на любом железе без GPU.
  Включён denoise (**OpenImageDenoise**) + adaptive sampling, `samples≈48`.
- ⚠️ Для headless-рендера на неизвестном железе по умолчанию — **Cycles CPU**.
  Eevee Next только если есть подтверждённо поддерживаемый GPU.
- ⚠️ Время: ~1–2 мин/изделие (CPU). Первый запуск дольше — «Loading denoising
  kernels». Это нормально. Ускорение — снизить `samples`/разрешение в
  `render/blender/build_scene.py: setup_render`.

---

## 7. Blender: пути вывода

- 🔴 Передавали **относительный** `out_dir` → Blender сохранял в `C:\output\...`
  (резолвил относительно своего cwd), драйвер ничего не находил → `RESULTS []`.
- ✅ В `render/blender_render.py` делаем `out_dir = os.path.abspath(out_dir)` и
  передаём абсолютные пути и для specs.json, и для папки вывода.
- ⚠️ В subprocess к Blender — **всегда абсолютные пути**.

---

## 8. Blender: API 4.2 (важные отличия версии)

### 8.1 `mesh.use_auto_smooth` удалён в 4.1+
- 🔴 `obj.data.use_auto_smooth = True` / `auto_smooth_angle` → AttributeError.
- ✅ Используем оператор `bpy.ops.object.shade_auto_smooth(angle=...)`
  (фолбэк — `shade_smooth`). Перед вызовом ставим объект активным.
- ⚠️ Плоские коробки **не** сглаживаем (flat shading + фаска). Авто-сглаживание —
  только для цилиндров.

### 8.2 Имена входов Principled BSDF 4.x
- ✅ `"Base Color"`, `"Roughness"`, `"Metallic"`, `"Specular IOR Level"`
  (раньше было просто `"Specular"`). Доступы к редким входам — через `if name in`.

### 8.3 Имя движка
- ✅ Идентификатор Cycles — `"CYCLES"`. (Eevee Next был бы `"BLENDER_EEVEE_NEXT"`.)

---

## 9. Blender: освещение и цвет (переэкспонирование)

- 🔴 Первые рендеры были «выбелены»: тёмный RAL 8019 выглядел почти белым,
  латунь — неоновой. Причина: энергия area-лайтов масштабировалась как `R*R`
  (для крупных изделий — тысячи ватт) → пересвет.
- ✅ Перешли на **SUN-лайты** (освещённость не зависит от расстояния/размера):
  key/fill/rim фиксированной силы + мягкий ambient world. Предсказуемая
  экспозиция для изделий любого габарита (`setup_lights`).
- ✅ `view_transform = 'Standard'` (а не AgX/Filmic) — цвета ближе к ТЗ.
- ✅ Цвет: sRGB-hex → **линейное** пространство (`_srgb_to_linear`) перед записью
  в `Base Color`. RAL/NCS — таблицей; «по согласованию» → тёплый крем.
- ⚠️ Не масштабировать мощность точечных/area-лайтов как функцию размера объекта.
  Либо SUN, либо мощность ∝ (расстояние²) при фиксированной освещённости.

---

## 10. Blender: геометрия (артефакты)

### 10.1 Чёрный «дырявый» верх рифлёного стола (z-fighting)
- 🔴 Верхняя грань сплошного ядра и верхняя грань крышки совпадали по `z=H`
  (копланарные перекрывающиеся грани) → чёрный шум в центре, чистым оставалось
  только кольцо-свес.
- ✅ Ядро делаем **чуть ниже** крышки: `core_h = H - top_th - 0.003`. Крышка —
  сплошной диск с лёгким свесом сверху.
- ⚠️ Любые две сплошные грани в одной плоскости = z-fighting. Разносить по высоте
  хотя бы на ~2–3 мм или прятать одну внутрь другой.

### 10.2 «Гофра» не читалась
- ✅ Флюты — это кольцо тонких вертикальных цилиндров, **выпирающих** за ядро:
  ядро `R*0.86`, флюты на `rr=R*0.90`, радиус флюта ~ полшага. Стоп ниже крышки.

### 10.3 Чёрные детали стола были не видны
- 🔴 Высокая фронтальная ¾-камера прячет содержимое ниши/задней части стола;
  кабель-канал и подвес ПК «исчезали».
- ✅ Для столов/тумб **опускаем камеру** (меньше вид сверху): elev 0.26–0.32,
  таргет выше; детали переносим в зону колен, видимую сквозь нишу.
- ⚠️ Ракурс подбирать под архетип: высокие шкафы — выше камера; столы — ниже.

### 10.4 Цоколь/«bowtie» в изометрии (исторически, в SVG-изометрии)
- ✅ Рисуем геометрически **видимые** грани проекции: top (`z=H`), front (`y=D`),
  side (`x=W`). Декор (двери, латунь, замок) — на плоскости `y=D`. Цоколь — заподлицо
  с корпусом, только front+side, без верхней плиты.

---

## 11. Композитинг рендера в лист

- ✅ В `draw/svg.py` добавлен `image(...)` (`href` + `xlink:href`); в корень SVG —
  `xmlns:xlink`. resvg рендерит и `href`, и `xlink:href`, и data-URI.
- ✅ PNG встраивается как **base64 data-URI** → SVG самодостаточен, PNG-экспорт
  через resvg работает (см. `archetypes/base.py: draw_hero`, `_png_size` читает
  размеры из IHDR без PIL).
- ✅ Архетипы: вместо векторной изометрии вызывают `draw_hero(...)`; если рендер
  есть — рисуют картинку и пропускают SVG-ISO (иначе фолбэк на изометрию).
- ⚠️ Метки панели («3D ИЗОМЕТРИЯ») рисуем **внутри** ветки фолбэка, а `draw_hero`
  ставит свою метку «3D ВИЗУАЛИЗАЦИЯ». Не дублировать подписи.

---

## 11b. Открытые двери/ящики + фурнитура (показ внутрянки)

Требование: на 3D всё, что открывается (двери шкафов/тумб, выдвижные ящики),
должно быть **открыто**, чтобы сразу видно наполнение. Плюс видимая фурнитура.

- ✅ **Корпус — «оболочка»**: строим зад + 2 бока + верх + низ, **без передней
  панели** (`build_casegood`). Внутри — полки; для шкафа со штангой ещё полка для
  шапок + штанга (горизонтальный цилиндр) + полка для обуви.
- ✅ **Двери распахнуты**: помощник `rotate_about_z(objs, px, py, angle)` крутит
  створку (вместе с латунной рамкой, ручкой, замком) вокруг кромки-петли.
  Левая дверь — петля на `-W/2`, угол `-115°`; правая/одиночная — на `+W/2`, `+115°`.
  ⚠️ Перед поворотом делаем `transform_apply(location,rotation,scale)` и крутим
  **вершины меша** `obj.data.transform(M)` — иначе произвольный пивот не учтётся.
- ✅ **Петли** (`_hinges`): 3 вертикальных стальных «бочонка» на кромке-петле.
  Добавляются **после** поворота двери (они — ось, не вращаются).
- ✅ **Ящики выдвинуты «лесенкой»** (`build_drawer_unit`): у каждого реальный
  лоток (дно + 2 борта + задник, открытый верх), верхние выдвинуты дальше.
- ✅ **Направляющие** (`runner…`): тонкие стальные рейки на боковых стенках тумбы
  на уровне каждого ящика — видны в зазоре, когда ящик выдвинут.
- ✅ Новый материал `steel` (`mat_metal("steel", "#8C9298", 0.38)`) для петель и
  направляющих.
- ⚠️ **Только один открытый элемент**: шкаф — только **правая** дверь (~95°);
  тумба с ящиками — только **нижний** ящик; направляющие/лоток — только у открытого.
- ⚠️ Штанга (`rod`): металлический прут `steel`, **без** лишней полки `sh_top`.
- ⚠️ **Кадрирование — по реальному bounding box** (`scene_bounds`), а не по
  `ШхГхВ`: открытые двери выходят за контур корпуса и иначе обрезаются. Камера
  получает `center` и `R` из фактических габаритов сцены (тень-кэтчер исключён).

## 11c. Ракурс камеры под функциональные элементы

- 🔴 У рабочего стола кабель-канал/подвес ПК — **сзади**; фронтальный ракурс их
  прячет, а пользователю надо их видеть.
- ✅ Ракурс задаётся **вектором направления на изделие** (`setup_camera(center,R,
  direction)`), индивидуально по архетипу. Для `desk_panel` — вид сзади-сбоку
  `(0.5, 1.0, 0.42)`; для остальных — фронтальный `(0.62, -1.0, elev)`.
- ⚠️ Правило: если у изделия есть важные элементы на нетипичной стороне — выбирай
  ракурс, где их **видно**, а не «красивый по умолчанию».
- ✅ Спецэлементы дублируем и на **2D-чертеже**: в `DeskPanel._front` кабель-канал
  (пунктирная вертикаль до пола) и подвес ПК (пунктирный прямоугольник) с
  подписями. `svg.rect` **не умеет dash** → пунктирные рамки рисуем `polyline`/`line`.

## 11d. Латунные вставки на опорах стола (баг «торчащих досок»)

- 🔴 Латунная полоса на ножке задавалась как `(0.012, D*0.9, h)` — глубина `D*0.9`
  по Y торчала вперёд «доской» почти на пол-глубины стола.
- ✅ Должна быть **тонкой по Y**: `(leg_w*0.5, 0.012, h)` на передней грани опоры
  (`y = -D/2 - 0.006`).
- ⚠️ Для накладного декора следи, какая ось «толщина»: декор на грани `±Y` тонкий
  по **Y**, на грани `±X` — тонкий по **X**. Иначе деталь протыкает/торчит.

## 11e. Пересвет светлых (белых/кремовых) поверхностей

- 🔴 Кремовые/NCS-светлые изделия «выбеливались» — форма терялась.
- ✅ Снизили свет: key `3.0→2.2`, fill `1.3→0.7`, rim `1.4→0.85`, world strength
  `0.7→0.45`; добавили запас экспозиции `view_settings.exposure = -0.25`.
- ⚠️ Правило экспозиции при `view_transform='Standard'` (жёсткий клиппинг светов):
  суммарная освещённость × альбедо светлого материала (крем ≈ 0.78 лин.) должна
  оставаться < 1, иначе клиппинг в белый. Тёмные (RAL 8019 ≈ 0.05) далеко от
  клиппинга — их это почти не затемняет. Если светлые всё ещё «горят» —
  уменьшать свет/экспозицию, либо рассмотреть `AgX` (мягкий рулёв светов, но
  десатурирует цвета).

## 11f. Масштаб 3D на листе

- 🔴 Шкафы казались мелкими → сделали `framing_radius` 0.62 + `slice` 1.04 →
  **перебор**: модель на весь экран, обрезает двери/стол/тумбы.
- ✅ Кадр: `framing_radius` + `CAM_MARGIN` per-arch (wardrobe 1.04, desk 1.00).
  На листе — только **`xMidYMid meet`**, **никогда slice**.
- ⚠️ Если мелко — чуть уменьшить margin (1.15); если обрезает — увеличить margin.
  Не комбинировать tight framing + slice.

## 11g. Гофрированный кофейный стол

- Гофра по **бокам на всю высоту тела** (до столешницы), **без** гладкого ободка снизу.
- **Сверху — сплошная столешница-диск** (рабочая поверхность), внутри — core
  (не полая труба). Не убирать top «ради гофры» — иначе некуда ставить предметы.

## 11h. Стол: отрыв 25 мм и подвес ПК

- 🔴 Не читался отрыв столешницы от опор; подвес ПК был по центру.
- ✅ Опоры `leg_d = D - 2*oh` (уже столешницы на 25 мм по глубине); столешница
  `(W+2oh)×(D+2oh)`. Подвес ПК: **`rx = W/2 - leg_w - 0.12`**, у правой опоры.
- ✅ На 2D: размер **25** (отрыв справа), подвес пунктиром у правой опоры.

## 12. Композиция листа (исторические правки)

- ✅ Блок «Материалы и описание» переполнял лист → `_clean_lines` фильтрует
  маркетинг и размеры (`^(габаритн\w*|размер\w*)`, чистые размерные строки),
  `draw_materials` уважает `max_h` и переносит строки.
- ✅ Высокие изделия (шкафы) в одной колонке давали сплющенную изометрию →
  адаптивная раскладка: 2 колонки (фронт+нутро слева, крупный 3D справа).

---

## 13. Поток данных (для ориентира)

```
doc_reader (таблица + картинки: DrawingML/VML)
  -> spec_parser (FurnitureSpec) + image_analysis/apply_image_cues (форма по фото)
  -> classifier (архетип)
  -> [Blender] blender_render -> build_scene (Cycles CPU, PNG на изделие)
  -> archetypes/* (2D-виды + draw_hero(3D) + размеры)
  -> draw/sheet (лист A4) -> render/export (PNG/галерея)
```

---

## 14. Чек-лист перед коммитом/запуском

1. Шелл-команды — под PowerShell, с UTF-8, из корня проекта.
2. Запуск: `python -m mebelgen "<docx>" --out output --png` (3D авто, если есть Blender).
3. Если 3D не появился — проверить `find_blender()` и что пути **абсолютные**.
4. Новый архетип Blender: плоские коробки без сглаживания; грани не копланарны;
   цвет через `_srgb_to_linear`; кадрирование по `scene_bounds` (не по ШхГхВ).
5. Не масштабировать свет под размер объекта (SUN); светлые материалы не должны
   гореть (см. §11e); тень — мягкая и маленькая (только key, угол ~17°, сверху).
6. Картинки Word: проверить и DrawingML, и VML.
7. Image-cue по форме — только для малых столов, не для шкафов/тумб.
8. **Всё, что открывается — открыто** (двери/ящики), с петлями и направляющими
   (§11b); ракурс выбирать так, чтобы были видны функциональные элементы (§11c),
   и дублировать их на 2D (пунктиром, `polyline`/`line` — у `svg.rect` нет dash).
9. Накладной декор: правильная ось «толщины» (на грани ±Y тонкий по Y, на ±X — по X).
10. 2D-заливки берём из `model/colors.palette_for` (синхрон с 3D), осветлённые.

## 15. Мета-правило (ОБЯЗАТЕЛЬНО)

Любая правка по итогам обратной связи пользователя или исправленная ошибка
**сразу** дописывается в этот файл (`DEVNOTES.md`): что было не так (🔴), как
исправлено (✅), какое правило на будущее (⚠️). Цель — со следующего раза делать
правильно без повторов. Не откладывать «на потом».
