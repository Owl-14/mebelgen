# --- Core (required) ---
python-docx>=1.1        # чтение таблиц .docx
Pillow>=10.0            # анализ изображений из ТЗ (форма: круг/флютинг)

# --- Legacy .doc support (Windows + установленный MS Word) ---
pywin32>=306 ; sys_platform == "win32"

# --- Растровый экспорт SVG -> PNG (опционально, но рекомендуется) ---
# resvg-py: самодостаточный рендерер на Rust, без системных библиотек.
resvg-py>=0.3

# Альтернативный конвертер (нужен системный Cairo); ставится по желанию:
# cairosvg>=2.7
