# Akeda Studio — baseline и parity-матрица

Связанная задача: `MEB-093`.

Статус: `REVIEW READY` — локальный эталон снят 2026-08-11 на базовом SHA
`a074825`. Документ фиксирует настоящий Studio до визуальных изменений. Он не
является макетом нового интерфейса. Полный набор кадров и воспроизводимые
сценарии: [STUDIO_BASELINE_REVIEW.md](STUDIO_BASELINE_REVIEW.md).

## Неприкосновенный контракт 3D

В рамках UX-эпика не меняются `src/webviewer.py`, `MebelScene`, payload сцены,
геометрия и визуальный результат мебели, свет, камеры, OrbitControls, raycast,
выбор, Shift+drag, анимации дверей и ящиков, присадки, фурнитура, размеры,
x-ray и exploded view.

Автоматический гейт любой визуальной ветки:

```bash
git diff --exit-code <base> -- tools/basis/src/webviewer.py
```

## Зафиксированный эталон

- Изделие: `Тумба для модератора`.
- Архетип: `cabinet`.
- Габариты: `1000×400×750`.
- Состав: 25 деталей, 241 присадка.
- Состояние: выбрана `Задняя стенка`; присадки, фурнитура, текстура и размеры
  включены; x-ray и exploded выключены.
- Страница: `src/studio.py::PAGE`.
- 3D: `src/webviewer.py::MebelScene`.

## Геометрия рабочего экрана

| Viewport | Левая панель | 3D/центр | Правая панель | Tabs/HUD |
|---|---:|---:|---:|---|
| 1280×720 | 300 px | 650 px | 330 px | не пересекаются |
| 1440×900 | 300 px | 810 px | 330 px | не пересекаются |
| 1920×1080 | 340 px | 1220 px | 360 px | не пересекаются |

На фактической встроенной ширине 1087 px центр получает 517 px, а постоянные
панели — 570 px. Исторически правая панель имела около 3900 px внутренней высоты;
локальный срез UX-025 делит этот поток на три режима. Левая панель также
переполняется при высоте 720–814 px.

## Карта зон

| Зона | Реальные функции | Ключевые DOM-контракты | Состояние аудита |
|---|---|---|---|
| Проект | выбор, новый, rename, duplicate, каталог | `projSel`, `projNew`, `projRen`, `projDup`, `projCat` | зафиксировано |
| Проверки и сводка | 7 статусов, ошибки, детали, присадки, стоимость, габариты, декор | `badges`, `errors`, `stats` | зафиксировано |
| Выбранная деталь | данные, координаты, apply/delete/drawing, команда словами | `partCard`, `ovApply`, `ovDelete`, `ovDetail`, `partChat` | зафиксировано |
| AI | provider, история, prompt, attachment, send, Undo, fix-all | `aiProvider`, `chatlog`, `chatMsg`, `chatAttach`, `chatSend`, `btnUndo`, `btnFixAll` | зафиксировано |
| Viewport | настоящий интерактивный `MebelScene` | `view3d`, `stage` | неприкосновенно |
| Режимы | 3D, чертёж, раскрой, печать | `tab3d`, `tabDraw`, `tabNest`, `btnPrint` | зафиксировано |
| HUD | 5 камер, 5 технических слоёв, open/close, exploded | `views`, `cbHoles`, `cbHw`, `cbTex`, `cbDims`, `cbXray`, `btnOpenAll`, `btnCloseAll`, `explode` | зафиксировано |
| Параметры | габариты, материалы, опоры, архетип, секции, инженерный raw ParamSpec | `f_w`, `f_d`, `f_h`, `f_color`, `f_facade_color`, `f_code`, `archSel`, `sections`, `rawspec`, `applyRaw` | зафиксировано |
| Производство | save, CFRN, B3D, delivery, versions | `btnSave`, `btnCfrn`, `btnB3d`, `btnDeliver`, `verSel`, `verRestore` | зафиксировано |
| Комплектация | выбор фурнитуры, смета, BOM | `hwPick`, `estTable`, `bom` | зафиксировано |
| Дополнительные экраны | каталог, чертёж, раскрой, empty state | `catalog`, `draw`, `emptyState` | чертёж и раскрой сняты; каталог/empty выделены в отдельные сценарии MEB-097 |

## Обязательные состояния для дальнейшей фиксации

| Состояние | Что проверяется | Статус |
|---|---|---|
| Обычный 3D | вращение, pan, zoom, камеры | кадры 1280/1440/1920 сняты; ручной review пользователя ожидается |
| Выбрана деталь | 3D highlight и карточка | кадр и контекст команды сняты |
| Открыты двери/ящики | индивидуальная и массовая анимация | массовое открытие снято; индивидуальное действие — в чек-листе ручного review |
| Технические слои | holes/hardware/texture/dimensions/x-ray | отображение слоёв и x-ray сняты |
| Exploded | слайдер и возврат в 0 | кадр разнесённого вида снят; возврат — в чек-листе ручного review |
| Чертёж | общий вид и деталировка | общий чертёж снят; деталировка — в чек-листе ручного review |
| Раскрой | листы, остатки, предупреждения | кадр раскроя снят |
| Каталог | поиск, категории, открытие | отдельный рабочий срез MEB-097; не является экраном этого baseline review |
| Empty | новый проект и импорт ТЗ | отдельный рабочий срез MEB-097; не является экраном этого baseline review |
| Validation error | ошибка и последняя валидная модель | кадр ошибки снят без сохранения изменённого изделия |
| Loading/busy | regenerate/chat/build | кадр `recalculating` снят через внешний локальный задержанный ответ API; production-код не менялся |

## Первые подтверждённые проблемы оболочки

1. Постоянные панели забирают больше половины ширины встроенного окна.
2. ~~Правая панель объединяет параметры, секции, экспорт, комплектацию, смету,
   BOM и raw JSON в один поток длиной около 3900 px.~~ Локально разделено на
   три режима UX-025; ожидает визуального review.
3. Левая панель смешивает управление проектом, диагностику, выбранную деталь и
   два интерфейса языковой команды.
4. Emoji/Unicode используются рядом с текстовыми кнопками без единой системы.
5. Состояния `изменено`, `пересчёт`, `последняя валидная модель`, `устарело` и
   `готово к производству` визуально не образуют единую модель.

## Принятое решение компоновки №1

Утверждён вариант A (`UX-017`): 3D является постоянным рабочим центром.

- до 1599 px правая панель по умолчанию свёрнута в rail шириной 48 px;
- rail открывает параметры, комплектацию или производство в существующей
  правой панели;
- на ширине от 1600 px обе панели по умолчанию открыты;
- ручной выбор пользователя сохраняется до перезагрузки страницы;
- текущий срез меняет только ширину и навигационную оболочку. Полноценный
  контекстный inspector выбранной детали выполняется отдельным review-срезом.

Фактическая встроенная ширина 1087 px теперь распределяется как 270 px слева,
769 px для центральной области и 48 px для rail. При открытии правой панели
центр возвращается к прежней ширине 517 px.

## Локальное решение компоновки №2

Три кнопки rail открывают не позицию в длинном документе, а отдельный режим:

- `Параметры` — конструкция изделия и инженерный raw ParamSpec;
- `Комплектация` — фурнитура, смета и BOM;
- `Производство` — сохранение, версии и передача результата.

В открытой панели те же режимы доступны через sticky tablist. Скрытые режимы
остаются в DOM для штатного обновления данных, но имеют `hidden + inert`.
