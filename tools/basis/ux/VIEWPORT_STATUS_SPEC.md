# Akeda Studio — нижняя строка состояния viewport

Статус: `MEB-098 APPROVED / MEB-101 IN PROGRESS`

Задачи: `MEB-098`, `MEB-101`
Обновлено: 2026-08-10

## Назначение

Строка отвечает на три коротких инженерных вопроса:

1. Текущая ревизия изделия уже пересчитана и показана в 3D или ещё нет?
2. Можно ли сейчас передавать данные в производство и экспорт?
3. Сохранены ли изменения изделия?
4. Какая деталь сейчас выбрана и какое действие доступно?

Это не строка ответа AI. Этап команды, результат, Undo и журнал остаются в
AI-composer и левой истории операций.

## Компоновка

Постоянная плоская полоса высотой `24 px` проходит по нижней границе `#main`.
Она имеет непрозрачный белый фон и только верхнюю линию, без скругления, тени,
blur или glass-эффекта.

Слева направо:

- маркер и текст состояния модели;
- независимое состояние `Сохранено / Есть несохранённые изменения`;
- `Выбрано · <имя детали>` — только при реальном выборе;
- контекстная подсказка активного режима;
- фиксированные единицы `мм`.

Только состояние модели является `aria-live`. Выбор и подсказки не озвучиваются
при каждом клике, чтобы не создавать речевой шум.

## Реальные состояния модели

| Фаза | Полный текст | Смысл |
|---|---|---|
| `changed` | `Изменения ещё не пересчитаны · показана предыдущая модель` | `SPEC` изменён, debounce ещё не запустил generate |
| `recalculating` | `Пересчитываю изменения · показана предыдущая модель` | активен актуальный `/api/generate` |
| `ready` | `Модель актуальна · производство доступно` | текущий viewer, проверки и позиции базы относятся к текущей редакции |
| `decision` | `3D актуальна · требуется решение` | модель построена, но не выбраны позиции производственной базы |
| `blocked` | `3D актуальна · производство заблокировано` | viewer текущий, но есть блокирующие инженерные проверки |
| `stale` | `Текущая редакция не построена · показана предыдущая модель` | viewer не получен либо пересчёт завершился ошибкой |
| `draft` | `Черновик · модель ещё не построена` | пустое изделие до генерации |

Поздний ответ старого request ID не имеет права менять строку. `schedule()`
немедленно переводит модель в `changed`, а `apply()` принимает финальное
состояние только при совпадении request ID и JSON-ревизии `SPEC`.

`Сохранено` — отдельная ось и не означает готовность модели. Например, сохранённая
редакция может быть `blocked`, а несохранённая — уже пересчитанной и `ready`.

## Производственный гейт

- `.cfrn`, `.b3d` и старый лист согласования доступны только в `ready`;
- browser передаёт SHA-256 ревизию последнего успешно показанного 3D payload;
- сервер сравнивает её с текущим `SPEC` и заново выполняет инженерные проверки;
- подмена клиентского состояния не позволяет обойти серверную проверку;
- ответ блокировки всегда содержит изделие, причину и следующее действие;
- при `changed`, `recalculating` и `stale` BOM, смета и производство явно помечены
  как данные предыдущей модели; старый центр никогда не выдаётся за новый результат.

## Контекстные подсказки

- 3D без выбора: `Клик — выбрать · перетаскивание — вращать · колесо — масштаб`;
- 3D с выбором: `Shift + перетаскивание — переместить · Esc — снять выбор`;
- чертёж: `Клик по детали — выбрать`;
- раскрой: `Раскрой текущей модели`.

Координаты курсора не показываются: реального cursor-coordinate API сейчас нет.

## Геометрия и адаптивность

- AI-composer поднят над status bar на `10 px`;
- toast остаётся ровно на `10 px` выше динамической высоты composer;
- чертёж получает динамический нижний padding, поэтому его можно прокрутить выше
  composer даже при длинном тексте и вложениях;
- status bar имеет `pointer-events:none` и не блокирует canvas;
- при ширине центра меньше `1000 px` используется короткий текст состояния;
- меньше `700 px` скрывается подсказка, но состояние, выбор и `мм` сохраняются;
- при критической ошибке/пересчёте на узкой ширине выбор уступает место состоянию.

## Связанные контракты

- SVG чертежа/раскроя привязан к `mode + request ID + JSON SPEC`; старая ревизия
  не разрешает печать;
- переименование изделия запускает пересчёт, потому что имя участвует в
  некоторых правилах генерации;
- динамическая высота composer синхронизируется сразу при росте текста и
  изменении вложений;
- `src/webviewer.py` и все внутренности `MebelScene` остаются неизменными.

## Review checklist

- [x] полоса воспринимается как системный status bar, а не ещё одна карточка;
- [x] `changed → recalculating → ready/decision/blocked/stale` визуально понятны;
- [x] сохранение и актуальность модели показаны независимо;
- [x] production-кнопки и сервер отклоняют stale/blocking редакцию;
- [x] выбор и снятие выбора обновляют только средний сегмент;
- [x] подсказка корректно меняется между 3D, чертежом и раскроем;
- [x] при открытом правом inspector строка не ломается;
- [x] composer, status bar, toast и прокрутка чертежа не перекрывают друг друга;
- [x] внешний вид и физика 3D-модели не изменились.
